import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  mainGrid: {
    padding: '40px',
    textAlign: 'center',
  },
  formTitle: {
    marginBottom: '20px',
  },
  form: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
}));
