import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  paper: {
    padding: '20px 0',
    textAlign: 'center',
    marginBottom: '10px',
  },
  rateLimit: {
    marginTop: '5px',
    textAlign: 'center',
  },
}));
