import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  paper: {
    padding: '5px',
    textAlign: 'center',
    marginBottom: '10px',
    display: 'flex',
    justifyContent: 'center',
  },
}));
