import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  mainGrid: {
    padding: '40px',
    textAlign: 'center',
  },
  formTitle: {
    marginBottom: '20px',
  },
  searchField: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'center',
  },
}));
